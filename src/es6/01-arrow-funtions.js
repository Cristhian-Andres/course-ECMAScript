// funcion normal.
function square(num){
    return num+num;
}

// como lo podemos hacer con una arrow funtions.
const square = (num) => {
    return num * num;
}

// Sin return y parentesis, simplifica el código. 
const squere = num => num * num;